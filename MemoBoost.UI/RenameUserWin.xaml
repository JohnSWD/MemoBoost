<Window x:Class="MemoBoost.UI.RenameUserWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MemoBoost.UI"
        mc:Ignorable="d"
        Title="RenameUserWin" Height="110" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <TextBlock Text="New name: " Margin="10" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Path=Name}" Name="nameBox" Margin="75 0 10 0" VerticalAlignment="Center"/>
        <Button Name="okButton" Content="Ok" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Center" Height="20" Margin="0 2 10 2" Width="60" Click="OkButton_Click"/>
    </Grid>
</Window>
